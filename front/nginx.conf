server {
    listen 80;
    server_name _;

    # 정적 리소스 위치
    root /usr/share/nginx/html;
    index index.html;

    # Static files + SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # -----------------------------
    # Python backend proxy
    # -----------------------------
    # /run/python → py-code-run ksvc로 전달
    location /run/python/ {
        proxy_pass http://py-code-run.default.svc.cluster.local/run;
        proxy_http_version 1.1;
        proxy_set_header Host py-code-run.default.svc.cluster.local;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 10s;
        proxy_read_timeout 120s;
    }

    # --------------TEST---------------
    # /test/ 요청 → helloworld-go ksvc로 전달하는 프록시
    # location /test/ {
    #     proxy_pass http://helloworld-go.default.svc.cluster.local/;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host helloworld-go.default.svc.cluster.local;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    #     proxy_connect_timeout 10s;
    #     proxy_read_timeout 120s;
    # }

    # Java backend (미사용, 예시)
    # location /run/java/ {
    #     proxy_pass http://java-run-service.default.svc.cluster.local/run;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_connect_timeout 10s;
    #     proxy_read_timeout 120s;
    # }
}

